<view class="index-page">
  <i-spin fix wx:if="{{ loading }}"></i-spin>

  <!-- 快捷功能：扫码、手动录入、读者荐购 -->
  <quick-area areas="{{ ['scan', 'hand', 'forward'] }}" bindscantap="onScanTap" />

  <i-toast id="toast" />
</view>
